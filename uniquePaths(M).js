var uniquePaths = function(row, column, rowPosition, columnPosition) {
  rowPosition = rowPosition || 0
  columnPosition = columnPosition || 0

  console.log(rowPosition, columnPosition)
}

uniquePaths(3, 2) //expect 3